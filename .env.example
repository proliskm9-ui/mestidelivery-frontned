# Backend API (production: empty = same origin)
VITE_API_URL=
VITE_PICTURE_API_URL=

# Google Sign-In is handled server-side via /api/auth/google/start (no Firebase SDK).

# Stability AI (legacy)
STABILITY_API_KEY=

# Replicate FLUX Fill Pro (scripts/sunset_v3_hot_batch.py generate)
REPLICATE_API_TOKEN=
